# GitLite

This is my first devops project is a simplified GitHub clone built using FastAPI as the backend framework. only used CRUD operation.
It allows CRUD operations for file management, supports PostgreSQL as the database, and includes Docker-based CI/CD automation.
"# GitLite" 
